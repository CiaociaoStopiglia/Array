let ranking = ["need for speed", "the sims", "Nba 2k25", "League of legends"];
let Favoritos = "Need for speed";
let i = 0;

for (i = 0; i < ranking.length; i++); {
    if (ranking[i] === Favoritos) {
        console.log(Favoritos + " está em " + (i + 1) + "° lugar") 
    }
}